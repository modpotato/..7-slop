[package]
name = "slop_ui"
version = "0.1.0"
edition = "2021"
description = "A tiny, feature-flagged, trait-first UI core in Rust."
license = "GPL-3.0-only"

[lib]
path = "src/lib.rs"

[features]
default = []
input = []
layout = []
text = []
